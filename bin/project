#!/usr/bin/env bash

function main(){
  local path="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
  local script=$(basename "$0")
  local exec=help
  case $1 in
    -h|--help)
      shift
      ;;
    *)
      ;;
  esac
  ${path}/${script}-$exec "$*"
}

main "$@"

unset -f main